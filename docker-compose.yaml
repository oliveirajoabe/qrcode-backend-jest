version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: short-links-qrcode
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: short-links-qrcode
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
